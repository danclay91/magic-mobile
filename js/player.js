export default function Player(defaultLife, ){

    var player = {
        name: "Player", 
        lifeTotal: 20,
        color:null,
        index:null, 
    }

    return player; 
}

/**
 * { name: 'Fred', lifeTotal: 20, color: null,
 */